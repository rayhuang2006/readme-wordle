# --- Python 基本過濾 (必備) ---
# 這是 Python 編譯出來的快取檔，不需要上傳
__pycache__/
*.pyc

# --- 虛擬環境 (絕對要過濾) ---
# 這些是你電腦本地安裝的套件，不應該上傳到 GitHub
venv/
.venv/
env/
.env

# --- 作業系統垃圾檔 ---
# Mac 和 Windows 會自動產生的縮圖或設定檔
.DS_Store
Thumbs.db

# --- 編輯器設定 (選填) ---
# 如果不想把你的 VS Code 設定傳上去
.vscode/
.idea/

# --- 專案特定設定 ---
# 本地測試時產生的暫存圖片，不需要上傳
test_output.png

# !!! 重要提醒 !!!
# 下面這兩個檔案我們 "故意不" 加進來，因為我們需要機器人更新它們：
# 1. wordle_status.png (顯示結果用)
# 2. state.json (紀錄答案和猜測次數用)